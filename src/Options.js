import React from "react";

function Options({ option }) {
  return (
    <div>
      {/* {options.map((option) => (
        <p>{option}</p>
      ))} */}
      {option}
    </div>
  );
}

export default Options;
